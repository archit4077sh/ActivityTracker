export class User{ // exporting a User class to make it available outside for get and push request.
  user_id : string;
  event_type : string;//event type can be Hover or Click 
  image_type : string;
  instance : string;//here intance is for Timestamp
}
